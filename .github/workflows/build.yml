name: Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.x

      - name: Restore dependencies
        run: dotnet restore Scanner.sln -p:Platform=x64

      - name: Inject OAuth credentials
        shell: pwsh
        env:
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
        run: |
          $file = "src/Scanner.Core/Services/GoogleDriveService.cs"
          (Get-Content $file) `
            -replace 'REPLACE_WITH_GOOGLE_CLIENT_ID', $env:GOOGLE_CLIENT_ID `
            -replace 'REPLACE_WITH_GOOGLE_CLIENT_SECRET', $env:GOOGLE_CLIENT_SECRET |
          Set-Content $file

      - name: Publish x64
        run: |
          dotnet publish src/Scanner.App/Scanner.App.csproj `
            -c Release `
            -p:Platform=x64 `
            -p:PublishProfile=win-x64 `
            -o publish/x64

      - name: Install Inno Setup
        run: choco install innosetup -y --no-progress

      - name: Extract version from tag
        id: version
        shell: bash
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            echo "version=0.0.0-dev" >> "$GITHUB_OUTPUT"
          fi

      - name: Build installer
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" `
            /DMyAppVersion="${{ steps.version.outputs.version }}" `
            installer\scanner-setup.iss

      - name: Create zip archive
        run: Compress-Archive -Path publish\x64\* -DestinationPath artifacts\Scanner-x64.zip

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: scanner-installers
          path: |
            artifacts/Scanner-Setup-x64.exe
            artifacts/Scanner-x64.zip

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/Scanner-Setup-x64.exe
            artifacts/Scanner-x64.zip
